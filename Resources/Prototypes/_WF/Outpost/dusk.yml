- type: gameMap
  id: Frontier
  mapName: 'Dusk Station'
  mapPath: /Maps/_WF/Outpost/dusk.yml
  minPlayers: 0
  maxPlayers: 100
  randomRotation: false
  maxRandomOffset: 0
  stations:
    Dusk:
      stationProto: StandardFrontierStation
      gridComponents:
      - type: VesselInfo
        description: "From the Dusk begins a new dawn."
      components:
      - type: StationNameSetup
        mapNameTemplate: 'Dusk Station'
      #          nameGenerator:
      #            !type:NanotrasenNameGenerator
      #            prefixCreator: '14'
      - type: ExtraStationInformation
        iconPath: /Textures/_WF/Logo/icon/icon-256x256.png
        stationSubtext: 'frontier-lobby-frontier-subtext'
        stationDescription: 'frontier-lobby-frontier-description'
        lobbySortOrder: 1
      - type: StationTransit
        routes:
          SpawnPoints: 20
      - type: StationDeadDrop
        maxDeadDrops: 8 # big station
      - type: StationJobs
        availableJobs:
          Wayfarer: [ -1, -1 ]
          Borg: [ -1, -1 ]
          StationRepresentative: [ 1, 1 ]
          StationTrafficController: [ 1, 1 ]
          Valet: [ 1, 1 ]
          NFJanitor: [ 1, 1 ]
          MailCarrier: [ 2, 2 ]
          MedicalER: [ 3, 3 ]
        tags:
        - HeadOfPersonnel
        - HeadOfSecurity
