// TODO: Maybe move this to somewhere else? I don't feels like putting this here is a good idea, but right now all functionality is that it allow for messages between maps, so it kinda fit?

namespace Content.Shared._WF.Radio.Components;

/// <summary>
/// Entities with <see cref="RAEWSDishComponent"/> are needed to transmit messages across maps.
/// </summary>
[RegisterComponent]
public sealed partial class RAEWSDishComponent : Component
{
}
