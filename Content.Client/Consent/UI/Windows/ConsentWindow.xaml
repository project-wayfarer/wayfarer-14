<controls:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    MinSize="620 670"
    Title="{Loc consent-window-title}"
    Resizable="False">
    <BoxContainer Orientation="Vertical" HorizontalExpand="True" Margin="5">
        <Label Text="{Loc consent-window-text}" />
        <BoxContainer Orientation="Horizontal" Margin="5">
            <Label Name="SaveLabel" FontColorOverride="Red" Access="Public" />
            <Control HorizontalExpand="True" />
            <Button
                Name="SaveConsentSettings"
                Access="Public"
                Text="Save"
                Disabled="True" />
        </BoxContainer>
        
        <TabContainer Name="ConsentTabContainer" Access="Public" VerticalExpand="True" HorizontalExpand="True">
            <!-- Character Specific Tab -->
            <BoxContainer Name="CharacterTab" Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True">
                <Label Text="{Loc consent-window-character-freetext-label}" />
                <PanelContainer HorizontalExpand="True" VerticalExpand="True" MinWidth="200">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
                    </PanelContainer.PanelOverride>
                    <TextEdit
                        Name="CharacterConsentFreetext"
                        Access="Public"
                        MinHeight="100"
                        VerticalExpand="True" />
                </PanelContainer>
            </BoxContainer>

            <!-- Account Specific Tab -->
            <BoxContainer Name="AccountTab" Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True">
                <Label Text="{Loc consent-window-account-freetext-label}" />
                <PanelContainer HorizontalExpand="True" VerticalExpand="True" MinWidth="200" MinHeight="100">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
                    </PanelContainer.PanelOverride>
                    <TextEdit
                        Name="ConsentFreetext"
                        Access="Public"
                        MinHeight="100"
                        VerticalExpand="True" />
                </PanelContainer>
                <Control MinSize="0 10" />
                <Label Text="{Loc consent-window-toggles-label}" />

                <ScrollContainer HorizontalExpand="True" VerticalExpand="True" HScrollEnabled="False" VScrollEnabled="True" MinHeight="50">
                    <BoxContainer Name="ConsentList" Access="Public" HorizontalExpand="True" SeparationOverride="2" Orientation="Vertical">
                        <!-- The rest here is generated programmatically -->
                    </BoxContainer>
                </ScrollContainer>
            </BoxContainer>

            <!-- Preview Tab -->
            <BoxContainer Name="PreviewTab" Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True">
                <Label Text="{Loc consent-window-preview-label}" />
                <PanelContainer HorizontalExpand="True" VerticalExpand="True" MinWidth="200">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
                    </PanelContainer.PanelOverride>
                    <ScrollContainer HorizontalExpand="True" VerticalExpand="True" HScrollEnabled="False" VScrollEnabled="True">
                        <RichTextLabel
                            Name="PreviewText"
                            Access="Public"
                            Margin="5" />
                    </ScrollContainer>
                </PanelContainer>
            </BoxContainer>
        </TabContainer>
    </BoxContainer>
</controls:FancyWindow>
